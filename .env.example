# JobPulse public API (optional — site falls back to sample jobs if offline)
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Contractor slug in JobPulse directory seed
NUXT_PUBLIC_CONTRACTOR_SLUG=red-clay-cabinet-installers

# JobPulse directory UI (optional deep links)
NUXT_PUBLIC_DIRECTORY_URL=http://localhost:3001
